name: Deploy PROD HRIS APPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: haris_apps
    environment: hris_apps
    env:
      WORKING_DIR: ${{ vars.WORKING_DIR_PROD }}

    steps:
      # =============================
      # Checkout Repository
      # =============================
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # =============================
      # Deploy Application
      # =============================
      - name: Deploy application
        run: |
          set -euo pipefail

          # Validasi working directory
          if [ ! -d "$WORKING_DIR" ]; then
            echo "❌ WORKING_DIR tidak ditemukan: $WORKING_DIR"
            exit 1
          fi

          echo "📂 Masuk ke directory deploy..."
          cd "$WORKING_DIR"

          echo "🔐 Ubah ownership sementara untuk deployment..."
          sudo chown -R "$(whoami)":"$(whoami)" .

          echo "🔄 Sync source code..."
          rsync -avz --delete "${GITHUB_WORKSPACE}/app/" app/
          rsync -avz --delete "${GITHUB_WORKSPACE}/routes/" routes/
          rsync -avz --delete "${GITHUB_WORKSPACE}/config/" config/
          rsync -avz --delete "${GITHUB_WORKSPACE}/resources/" resources/
          rsync -avz --delete --exclude='cache/*' "${GITHUB_WORKSPACE}/bootstrap/" bootstrap/
          rsync -avz --delete "${GITHUB_WORKSPACE}/public/assets/" public/assets/
          rsync -avz "${GITHUB_WORKSPACE}/composer.json" "${GITHUB_WORKSPACE}/composer.lock" ./

          echo "🧹 Bersihkan cache dan vendor lama..."
          rm -rf vendor/
          rm -rf bootstrap/cache/*.php || true

          echo "📦 Install PHP dependencies (production only)..."
          composer install \
            --no-dev \
            --optimize-autoloader \
            --no-interaction \
            --prefer-dist \
            --classmap-authoritative

          echo "🎨 Install & build frontend..."
          npm ci
          npm run build

          echo "🔐 Set permission akhir (Laravel-safe)..."

          # Pastikan folder runtime ada (Laravel butuh ini writable)
          sudo mkdir -p \
            storage/logs \
            storage/framework/cache \
            storage/framework/sessions \
            storage/framework/views \
            bootstrap/cache

          # Pastikan file log ada
          sudo touch storage/logs/laravel.log || true

          # Ownership runtime folder untuk web user
          sudo chown -R www-data:www-data storage bootstrap/cache

          # Permission aman: rw untuk owner+group, others read/execute seperlunya
          sudo chmod -R ug+rwX storage bootstrap/cache

          # Setgid agar file/dir baru mewarisi group (mengurangi masalah permission)
          sudo find storage bootstrap/cache -type d -exec chmod g+s {} \;

          # Public read-only standar
          sudo chown -R www-data:www-data public
          sudo find public -type d -exec chmod 755 {} \;
          sudo find public -type f -exec chmod 644 {} \;

          echo "⚡ Optimize Laravel (jalankan sebagai www-data agar tidak gagal log/cache)..."
          sudo -u www-data php artisan optimize:clear
          sudo -u www-data php artisan optimize

          # Pastikan lagi setelah artisan (kadang ada file baru)
          sudo chown -R www-data:www-data storage bootstrap/cache
          sudo chmod -R ug+rwX storage bootstrap/cache

          echo "🔄 Restart queue worker..."
          sudo supervisorctl restart laravel-queue:* || true

          echo "✅ Deploy selesai!"
